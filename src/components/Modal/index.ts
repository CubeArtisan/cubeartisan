import {
  ModalCloseButton as CloseButton,
  ModalContent as Content,
  ModalDescription as Description,
  ModalOverlay as Overlay,
  ModalPortal as Portal,
  ModalRoot as Root,
  ModalTitle as Title,
  ModalTrigger as Trigger,
} from '@cubeartisan/cubeartisan/components/Modal/Modal';

export const Modal = { Root, Overlay, Content, Title, Description, Portal, Trigger, CloseButton };
