import {
  SwitchControl as Control,
  SwitchInput as Input,
  SwitchLabel as Label,
  SwitchRoot as Root,
  SwitchThumb as Thumb,
} from '@cubeartisan/cubeartisan/components/Switch/Switch';

export const Switch = { Root, Input, Label, Control, Thumb };
