{
  "name": "cubeartisan",
  "version": "0.1.0",
  "description": "A cube management and draft service for Magic the Gathering.",
  "private": true,
  "workspaces": [
    "jobs",
    "next"
  ],
  "sideEffects": [
    "**/RenderToRoot.js",
    "**/pages/*.js"
  ],
  "license": "AGPL-3.0-only",
  "repository": "https://github.com/CubeArtisan/cubeartisan",
  "scripts": {
    "g:list-files": "cd $INIT_CWD && git ls-files '*.js' '*.cjs' '*.mjs' '*.jsx' '*.ts' '*.tsx'",
    "g:list-all-files": "cd $PROJECT_CWD && git ls-files '*.js' '*.cjs' '*.mjs' '*.jsx' '*.ts' '*.tsx'",
    "g:lint": "cd $INIT_CWD && pwd && sh -c 'yarn run -B eslint --quiet `yarn run --silent g:list-files`'",
    "g:lint-fix": "cd $INIT_CWD && sh -c 'yarn run -B eslint --fix `yarn run --silent g:list-files`'",
    "g:lint-loud": "cd $INIT_CWD && sh -c 'yarn run -B eslint `yarn run --silent g:list-files`'",
    "lint": "yarn workspaces foreach -p --include '@cubeartisan/*' run g:lint",
    "lint-fix": "yarn workspaces foreach -p --include '@cubeartisan/*' run g:lint-fix",
    "lint-loud": "yarn workspaces foreach -p --include '@cubeartisan/*'run g:lint-loud",
    "cards": "yarn workspace @cubeartisan/jobs cards",
    "checktypes": "yarn pnpify tsc -p tsconfig.json",
    "ci": "yarn nearley; yarn lint && yarn checktypes",
    "nearley": "yarn workspace @cubeartisan/filter nearley",
    "dev": "yarn workspace @cubeartisan/next dev",
    "build": "yarn workspace @cubeartisan/next build",
    "start": "yarn workspace @cubeartisan/next start"
  },
  "engines": {
    "node": "18"
  },
  "packageManager": "yarn@3.2.4",
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^5.42.0",
    "@typescript-eslint/parser": "^5.42.0",
    "eslint": "^8.26.0",
    "eslint-config-next": "13.0.1",
    "eslint-plugin-array-func": "^3.1.7",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  },
  "devDependencies": {
    "@types/prettier": "^2"
  }
}
